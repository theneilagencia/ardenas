# Contratos transversais de infraestrutura (provider-neutros)

- Manifesto de decisão: `config/infrastructure/production-decision.example.yaml`
  (+ schema em `tooling/infrastructure/decision-manifest.ts`).
- IAM abstrato: `tooling/infrastructure/iam-contracts.ts`.
- Rede/egress: `tooling/infrastructure/network-catalog.ts`.
- Recuperação de banco: `tooling/infrastructure/database-recovery.ts`.
- Orçamento de conexões: `tooling/infrastructure/connection-budget.ts`.
- Artefato imutável: `tooling/infrastructure/artifact-manifest.ts`.
