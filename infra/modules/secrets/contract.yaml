# ARDEN-PRD-001.2A.2 — Contrato do módulo secrets (provider-neutro, NÃO executável).
module: secrets
inputs:
  selectedProduct: TBD
  identityReferences: TBD
  platformSecretCatalog: [DATABASE_URL, CONNECTOR_MASTER_KEY_PRIMARY, CONNECTOR_MASTER_KEY_BACKUP_WRAPPING_KEY, SESSION_JWT_MATERIAL, OBSERVABILITY_TOKEN]
  recoveryPolicy: TBD
  deletionProtection: true
outputs:
  secretReferences: <ref[]>
  adapterConfiguration: EXTERNAL_SECRET_MANAGER
  auditReference: <ref>
invariants:
  noRealSecretInManifest: true
  versioned: true
  leastPrivilegePerIdentity: true
  wrappingKeyReadableOnlyByRestoreOperator: true
